# unpack_probe

This directory holds the minimal unpack-chain verification for `samples/pev70.apk`.

Run:

```bash
python3 src/research/unpack_probe/probe_unpack.py
```

Outputs are written to `cache/unpack_probe/`, including extracted native assets, dex slices, and `report.md`.
