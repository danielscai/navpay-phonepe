# 步骤 1-4 测试记录

日期：2026-01-30

本文记录当前进度下步骤 1-4 的**真实运行验证**与可复用脚本。

## 步骤 1：从真实手机中获取 APK 文件（已完成，已验证）

### 目标
从真实设备重新拉取 APK（含 split），并校验文件完整性。

### 执行命令
```bash
./tools/step1_verify_extract_apk.sh -s SM02G4061933188
```

### 结果
- 已通过 `adb pull` 重新拉取到 `temp/phonepe_from_device/`
- `base.apk`、`split_config.arm64_v8a.apk`、`split_config.xxhdpi.apk` 均通过 zip 完整性检查

## 步骤 2：合并 APK 文件到一个单体 APK（已完成，已验证）

### 目标
使用真实合并流程生成单体 APK，并验证签名。

### 执行命令
```bash
./tools/step2_verify_merge_apk.sh
```

### 结果
- 已重新执行合并流程（apktool 反编译 → 合并 → 重新打包 → zipalign → 签名）
- 产物：`temp/phonepe_merged_test/com.phonepe.app_merged_signed.apk`
- `apksigner verify` 显示 v2/v3 签名有效

## 步骤 3：签名绕过（已完成，已验证）

### 目标
重新编译 smali，注入到合并 APK，重新打包、签名，并安装到模拟器运行。

### 执行命令
```bash
./tools/step3_verify_signature_bypass.sh --install --run -s emulator-5554
```

### 结果
- 重新构建 `signature_bypass` 产物（DEX/Smali/Pine Smali）
- 使用 `apktool` 反编译合并 APK → 注入 Hook → 重新打包 → zipalign → apksigner 签名
- 安装到模拟器 `emulator-5554` 并启动主界面活动：
  `com.phonepe.app/.launch.core.main.ui.MainActivity`

### 备注
- 编译过程有 JDK 版本相关 warning（不影响产物生成）
- apktool 会保留 `PhonePeApplication.smali.bak`，构建时提示忽略该文件（正常）

## 步骤 4：HTTPS 请求拦截（代码完成，已做真实运行验证）

### 目标
真实构建、安装并启动应用（模拟器环境）。

### 执行命令
```bash
./tools/step4_test_https_interceptor.sh --build --install --run -s emulator-5554
```

### 结果
- 构建成功，生成 `src/apk/https_interceptor/app/build/outputs/apk/debug/app-debug.apk`
- 已安装并启动到模拟器 `emulator-5554`
- **未执行实际拦截流量验证**（需要手动输入 URL 并检查日志）

### 手动验证建议
```bash
./tools/step4_test_https_interceptor.sh --logs -s emulator-5554
# 在 App 内触发请求后观察 logcat
```

## 脚本清单（按步骤编号）

- `tools/step1_verify_extract_apk.sh`
- `tools/step2_verify_merge_apk.sh`
- `tools/step3_verify_signature_bypass.sh`
- `tools/step4_test_https_interceptor.sh`

所有脚本均会先**清理旧输出**再执行，以确保验证为真实运行结果。
