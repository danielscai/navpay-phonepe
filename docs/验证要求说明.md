# 验证要求说明

日期：2026-01-30

以下为当前确认的统一验证要求，后续所有步骤均按此执行：

1. **必须真实执行**：验证不得只检查文件是否存在，必须跑完完整流程。
2. **必须清理旧输出**：每次验证前删除旧产物，确保结果来自本次真实执行。
3. **步骤 1 必须重新 adb 拉取**：从真机使用 `adb pull` 获取 APK，并完成完整性校验。
4. **步骤 2 必须真实合并**：完整执行 apktool 解包、合并、重打包、zipalign、签名验证。
5. **步骤 3 必须全流程**：编译 smali → 注入 APK → 重打包 → 签名 → 安装到模拟器 → 启动验证。
6. **步骤 4 必须在模拟器中通过**：只有在模拟器安装并运行、功能验证通过后才算完成。
7. **每个步骤都要记录**：必须输出可复用脚本，并写入测试记录文档。
