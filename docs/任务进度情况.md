1. 从真实手机中获取 APK 文件（已完成）：输出目录 `temp/phonepe_from_device`（已通过脚本重新拉取并验证）。
2. 合并 APK 文件到一个单体 APK（已完成）：输出目录 `temp/phonepe_merged_test`（已重新合并并验证签名）。
3. 签名绕过（已完成）：Pine Hook `PackageManager.getPackageInfo()`，构建产物可生成（已验证）。
4. HTTPS 请求拦截（已完成）：demo 位于 `src/https_interceptor/`，已完成构建/安装/启动验证，adb 与 log server 均可确认拦截日志。

最近测试记录：`docs/步骤1-4_测试记录.md`。
