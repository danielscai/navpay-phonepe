1. 从真实手机中获取 APK 文件（已完成）：输出目录 `temp/phonepe_from_device`（已通过脚本重新拉取并验证）。
2. 合并 APK 文件到一个单体 APK（已完成）：输出目录 `temp/phonepe_merged_test`（已重新合并并验证签名）。
3. 签名绕过（已完成）：Pine Hook `PackageManager.getPackageInfo()`，构建产物可生成（已验证）。
4. HTTPS 请求拦截（已完成）：demo 位于 `src/https_interceptor/`，已完成构建/安装/启动验证，adb 与 log server 均可确认拦截日志。
5. 自动化登录过程（已完成）：脚本 `tools/step5_auto_login.sh`，可自动输入手机号并点击 Proceed 进入 OTP 页面。

最近测试记录：`docs/步骤1-4_测试记录.md`。

---

## 下一步实现计划（基于 pev70 分析）

### A. 可选（待定，暂不实施）
1. A1：加密前/解密后明文拦截（对应 RequestEncryptionInterceptor / RequestEncryptionUtils）。
2. A2：WebSocket 监听（LoggingWebSocketListener）。
3. A3：应用层 JSON Hook（GenericRestData.setBodyJSON）。

> 说明：A1/A2/A3 可能与当前网络层日志能力有重叠，先保留为“待定”，后续按需要再决定是否实现。

### B. 需要登录（后续实施，按顺序）
1. B4：Token 相关 Hook（PhonePeInterceptor / Token 抓取与保存）。
2. B5：Dagger SingletonC 劫持（generatedComponent → PhonePeHelper 访问 Token/用户信息）。
3. B6：UPI 列表与 UPI Meta（getUPIs / getUPIRequestMeta）。

### C. 高风险/交互依赖（最后阶段）
1. C7：MPIN / UPI PIN / OTP 拦截。
2. C8：Activity 截图上传 / 数据备份。
3. C9：Syncclient 双向 Token 写入 / 账户接管。
